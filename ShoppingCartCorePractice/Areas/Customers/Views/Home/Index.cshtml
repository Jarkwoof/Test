@model IEnumerable<ShoppingCartCorePractice.Migrations.Products>
@{
    ViewData["Title"] = "Index";
}

<style>
    .row {
        display: flex;
    }

    .col {
        margin: 10px;
    }
</style>

<div class="row" id="app">
    @*@if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
                <div class="col">
                    <div class="card" style="width: 20rem;">
                        <img class="card-img-top" src="~/@item.PhotoUrl">
                        <div class="card-block">
                            <h4 class="card-title">@item.Name</h4>
                            <p class="card-text">@item.Price</p>
                            <a href="#" data-name="Orange" data-price="0.5" class="add-to-cart btn btn-primary">Add to cart</a>
                        </div>
                    </div>
                </div>
            }
        }*@
    <div class="col" v-for="(item , index) in list" :key="item.id">
        <div class="card" style="width: 20rem;">
            <img class="card-img-top" :src="item.photoUrl">
            <div class="card-block">
                <h4 class="card-title">{{item.name}}</h4>
                <p class="card-text">{{item.price}}</p>
                <a href="#" :id="item.id" data-price="0.5" class="add-to-cart btn btn-primary">Add to cart</a>
            </div>
        </div>
    </div>
</div>

<script>
     Vue.createApp({
        data() {
            return {
                list: [],
            }
        },
        mounted() {
            axios
                .get('https://localhost:44379/Customers/ShoppingProductApi/Index')
                .then((response => { (this.list = response.data); console.log(response.data)}))
                .catch(function (error) {
                    console.log(error);
                });
        },
        methods: {
            @*Edit(id) {
                window.location = '@Url.Content("~/Admin/ProductType/Edit/")' + id;
            },
            Delete(id) {
                window.location = '@Url.Content("~/Admin/ProductType/Delete/")' + id;
            }*@
        }
    }).mount('#app')
</script>
